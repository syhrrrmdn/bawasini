import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Developer Tools Suite — Bulk API & Image Converter",
  description:
    "Kumpulan tool developer: Bulk API Sender untuk kirim request massal, dan Image Converter untuk convert gambar JPG/PNG/WebP dengan support ZIP bulk.",
  keywords: ["bulk api sender", "image converter", "developer tools", "nextjs"],
  authors: [{ name: "Developer Tools" }],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="id"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
